# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'TravelAPIApp' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for TravelAPIApp
   pod 'Alamofire', '~> 4.8.0'
   pod 'SwiftyJSON', '~> 4.0'

end

target 'TravelAPIApp WatchKit App' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for TravelAPIApp WatchKit App
     pod 'Alamofire', '~> 4.8.0'
     pod 'SwiftyJSON', '~> 4.0'

end

target 'TravelAPIApp WatchKit Extension' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for TravelAPIApp WatchKit Extension
   pod 'Alamofire', '~> 4.8.0'
   pod 'SwiftyJSON', '~> 4.0'

end
